# RC10 — исключение отдельных тем ГИС ЖКХ из АСУД

В этой версии обращения ГИС ЖКХ по темам 2.1, 2.2, 2.3, 2.16, 2.25,
5.1, 5.2 и 23 вообще не создаются и не регистрируются в АСУД.

- Сопоставление выполняется строго по полному нормализованному названию темы.
- Проверка срабатывает до первого действия с карточкой документа; карточка не
  открывается и не заполняется.
- В реестр резолюций исключённые обращения не записываются.
- Исходный `.msg` остаётся в корне, чтобы выгрузчик ГИС ЖКХ не скачал его
  повторно.
- Рядом атомарно создаётся `<имя>.msg.asud_terminal.json`; монитор АСУД
  больше не включает такое письмо в очередь.
- Остальные темы ГИС ЖКХ регистрируются по прежнему маршруту на Басманова.
- Старый пользовательский конфиг RC9 с маршрутом Жукова не может отключить
  встроенное исключение этих восьми тем.

Проверка: 132/132 автоматических теста, `compileall`, `git diff --check`.
